1.抽象方法只存在于抽象类中，抽象类的非抽象子类必须实现抽象类的抽象方法；
2.父类中protected修饰的成员只能被父类和其子类调用；
3.当方法的参数类型为抽象类时，实际传入的是抽象类子类的对象，
在方法中调用对象的成员时：
1）如果调用的是抽象方法，则调用的是子类中实现的方法；
2）如果调用的不是抽象方法，不管子类有没有覆盖，调用的都是父类中的方法；
3）如果调用的是其他类型的成员，则调用的是父类的成员（父类都会实现）。
